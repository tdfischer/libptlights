cmake_minimum_required(VERSION 2.8)

set(PTLIGHTS_VERSION_MAJOR 0)
set(PTLIGHTS_VERSION_MINOR 1)
set(PTLIGHTS_VERSION_PATCH 0)

include_directories($CMAKE_CURRENT_SOURCE_DIR)
add_subdirectory(ptlights)
